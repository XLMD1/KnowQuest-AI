spring:
  datasource:
    url: jdbc:h2:mem:zhiven_test;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  ai:
    openai:
      api-key: test-key
      embedding:
        enabled: false

DASHSCOPE_API_KEY: test-key
